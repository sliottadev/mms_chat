import { View } from "react-native"
import { Image, Text } from "react-native-svg"

const UserHeader = ()=>{
    return(
        <View>
            <Image></Image>
            <Text>{'{Nombre_de_Usuario}'}</Text>
        </View>
    )
}

export default UserHeader;